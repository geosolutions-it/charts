apiVersion: v1
kind: Service
metadata:
  name: standalone-hz
  labels:
    {{- include "geoserver.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "geoserver.labels" . | nindent 4 }}
  ports:
    - protocol: TCP
      port: 5702
      targetPort: 5702
  type: ClusterIP
